name: CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install Django app deps
        run: |
          cd django-app
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Run Django migrations (test)
        run: |
          cd django-app
          python manage.py migrate
      - name: Run basic test (import)
        run: |
          cd django-app
          python - <<PY
import django, os
os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'django_project.settings')
django.setup()
print("Django imported OK")
PY

  deploy-heroku:
    if: github.ref == 'refs/heads/main'
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Heroku
        env:
          HEROKU_API_KEY: ${{ secrets.HEROKU_API_KEY }}
          HEROKU_APP_NAME: ${{ secrets.HEROKU_APP_NAME }}
        run: |
          echo "Deploy step here — use Heroku CLI or GitHub integration."
          # Example (uncomment after adding secrets and setting up heroku):
          # git remote add heroku https://git.heroku.com/${HEROKU_APP_NAME}.git
          # git push heroku main
